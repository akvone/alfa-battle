logging:
  level:
    root: INFO

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: postgres
    password: postgres
    hikari:
      minimum-idle: 10
#  jpa:
#    hibernate:
#      ddl-auto: update